# GOAL:   list all audio files. 

## Directories
Audio files are located here:
- ~/mp3_downloads

Store all *.R files and anything related:
- ~/code/jimTools/R


## Tools
Use R language.   Use tidyverse where useful. 
Use R scripts (Rscript ...), which the USER will run from command line.

As needed, use R script, which requires tidyverse packages: `tibble`, `dplyr`, `stringr`.  Install these R packages only if missing.

## NEVER

Never use bash or zsh script files

## Output
Create and save an Rscript file (*.R) called audio_files.R which saves names of all audio files in a tibble.   
Call the tibble "audio_files" and have this structure:

- directory
- file name

The user will  run Rscript at command line.

If this file exists, write over it.

## When to STOP
When you read any of these words on a single line, in capital letters,  you stop reading and processing.   
Do nothing until user instructs you.


- "STOP"
- "END"
- "WAIT FOR USER"


 
STOP


